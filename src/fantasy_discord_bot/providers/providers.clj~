(ns fantasy-discord-bot.providers.providers
  (:require [fantasy-discord-bot.providers.yahoo.yahoo :as yahoo]))

(defmulti init-provider :provider)

(defmethod init-provider :yahoo
  [config]
  (yahoo/init-provider config))
